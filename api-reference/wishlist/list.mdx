---
title: 'List wishlist items'
description: 'Retrieve all wishlist entries for the authenticated user, with product data populated.'
sidebarTitle: 'List wishlist'
---

<Note>
  This endpoint requires authentication. Include a valid JWT in the `Authorization` header.
</Note>

## Query parameters

<ParamField query="filters[user][id][$eq]" type="number" required>
  Filter wishlist entries by the authenticated user's ID.
</ParamField>

<ParamField query="populate" type="string" default="product">
  Relations to populate in the response. Pass `product` to include full product data.
</ParamField>

## Request headers

<ParamField header="Authorization" type="string" required>
  Bearer token. Format: `Bearer <jwt_token>`
</ParamField>

<ParamField header="Content-Type" type="string" required default="application/json">
  Must be `application/json`.
</ParamField>

## Response fields

<ResponseField name="data" type="object[]">
  Array of wishlist entry objects.
  <Expandable title="wishlist entry">
    <ResponseField name="id" type="number">
      Unique identifier for the wishlist entry.
    </ResponseField>
    <ResponseField name="attributes" type="object">
      <Expandable title="attributes">
        <ResponseField name="createdAt" type="string">
          ISO 8601 timestamp of when the entry was created.
        </ResponseField>
        <ResponseField name="updatedAt" type="string">
          ISO 8601 timestamp of the last update.
        </ResponseField>
        <ResponseField name="product" type="object">
          Populated product data. Only present when `populate=product` is passed.
          <Expandable title="product">
            <ResponseField name="data" type="object">
              <Expandable title="product data">
                <ResponseField name="id" type="number">Product ID.</ResponseField>
                <ResponseField name="attributes" type="object">Product attributes (name, price, description, etc.).</ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata.
  <Expandable title="meta">
    <ResponseField name="pagination" type="object">
      <Expandable title="pagination">
        <ResponseField name="page" type="number">Current page number.</ResponseField>
        <ResponseField name="pageSize" type="number">Number of results per page.</ResponseField>
        <ResponseField name="pageCount" type="number">Total number of pages.</ResponseField>
        <ResponseField name="total" type="number">Total number of results.</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Examples

<CodeGroup>

```bash cURL
curl --request GET \
  --url 'http://localhost:1337/api/wishlists?filters[user][id][$eq]=1&populate=product' \
  --header 'Authorization: Bearer <jwt_token>' \
  --header 'Content-Type: application/json'
```

</CodeGroup>

### Response

```json 200
{
  "data": [
    {
      "id": 3,
      "attributes": {
        "createdAt": "2024-01-15T10:30:00.000Z",
        "updatedAt": "2024-01-15T10:30:00.000Z",
        "product": {
          "data": {
            "id": 5,
            "attributes": {
              "name": "Ethiopian Yirgacheffe",
              "price": 18.99,
              "description": "A bright, floral single-origin coffee.",
              "createdAt": "2024-01-10T08:00:00.000Z",
              "updatedAt": "2024-01-10T08:00:00.000Z"
            }
          }
        }
      }
    }
  ],
  "meta": {
    "pagination": {
      "page": 1,
      "pageSize": 25,
      "pageCount": 1,
      "total": 1
    }
  }
}
```
